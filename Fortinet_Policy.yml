- name: Aplicar políticas al FortiGate desde Git
  hosts: "{{ _hosts }}"
  gather_facts: no
  connection: httpapi
  roles:
    - role: firewall_policy
      vars:
        policy_file: "{{ _policy }}"