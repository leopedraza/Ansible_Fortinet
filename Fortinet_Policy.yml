- name: Aplicar políticas al FortiGate desde Git
  gather_facts: no
  connection: httpapi
  roles:
    - role: firewall_policy
      vars:
        hosts: "{{ _hosts }}"
        policy_file: "{{ _policy }}"